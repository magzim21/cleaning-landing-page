{
  "framework": null,
  "buildCommand": "node -e \"const fs=require('fs');const sha=(process.env.VERCEL_GIT_COMMIT_SHA||'').slice(0,7);fs.writeFileSync('build-info.json',JSON.stringify({shortSha:sha})+'\\n');\"",
  "outputDirectory": "."
}