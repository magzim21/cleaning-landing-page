Requirements:
- Make a mobile-first landing page for <TOPIC>.
- The CTA button should not wrap text.
- Scale out mobile version by 20% comparing to what most websites.
- Make sure the vertical gaps between sections are narrow, but distinct.
- Add Elegant animations to cards. Keep animations tasteful and include reduced-motion accessibility fallback.
- Promo line/ticker:
  - must be infinite;
  - add more spacing between repeated messages on desktop;
  - make ticker 2x slower.
- Add git commit sha (generated) to the very button of the footer. Barely visible.
Delivery expectations:
- Implement directly in `index.html`.
- Preserve existing content/branding unless needed for layout/function fixes.
- Verify responsive behavior across mobile, tablet (iPad), and desktop.
